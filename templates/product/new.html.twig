{% extends 'base.html.twig' %}

{% block title %}Ajout Brochures PDF{% endblock %}

{% block body %}

    <div class="container text-center mt-5 bg-#229296" style="width: 600px;border:solid 5px #229296;border-radius:50px;">
        <p class="text-center fs-2 fst-italic text-uppercase py-2" style="color:white;">
            <strong>Ajouter une nouvelle brochure</strong>
        </p>
    </div>

    <div class="container-xl">

		<div class="m-5" style="border:solid 25px #229296;border-radius:50px;">

			<div class="col-12 col-lg-12 bg-light px-lg-5" style="height:350px;border:solid 25px light;border-radius:25px;">

                {% if is_granted('ROLE_ADMIN') %}
                    {{ form_start(form) }}
                        {# ... #}

                        {{ form_row(form.brochure) }}
                        {{ include('admin/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
                            {# <div class="card-footer d-flex d-row justify-content-end p-1">
                                {{ include('product/_delete_form_product.html.twig') }}
                            </div> #}
                    {{ form_end(form) }}
                {% endif %}

			</div>

		</div>

        <div class="m-5">
            <a class="text-decoration-none text-light fs-4 fst-italic py-2 px-3 m-5" style="border:solid 2px #73e0ed;background-color:#229296;border-radius:25px;" href="{{ path('app_main_home') }}">
                <strong>Retour à l'accueil</strong>
            </a>
        </div>

	</div>

{% endblock %}