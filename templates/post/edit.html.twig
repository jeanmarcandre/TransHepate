{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}

    <div class="text-dark bg-light my-5 p-4 d-block w-100">
    <h1 class="mx-auto fs-2 text-uppercase fst-italic" style="width: 400px;"><strong>Modifier l'article</strong></h1>

    {{ include('post/_form.html.twig', {'button_label': 'Modifier cette Publication'}) }}

    <a href="{{ path('app_post_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('post/_delete_form.html.twig') }}
    </div>
{% endblock %}
