{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
    <h1 class="text-light">Create new Post</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Nouvelle Publication') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_post_index') }}">back to list</a>
{% endblock %}
