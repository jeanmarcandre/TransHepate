{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}

    <div class="container">

    <div class="my-5 text-center">
        <span class="fs-2 px-3 py-1 fst-italic text-light text-uppercase"
            style="border:solid 5px #73e0ed;border-radius:25px;background-color:#229296">
            <strong>Veuillez-vous connecter</strong>
        </span>
    </div>

        <div class="mt-5"  style="border:solid 25px #229296;border-radius:50px;margin-bottom:100px;">

            <div class="col-12 col-lg-12 bg-light px-2 px-lg-5" style="border:solid 5px light;border-radius:25px;">


                <form method="post" class="text-dark bg-light p-4 d-block w-100">
                {% if error %}
                <div id="loginMessages" class="alert alert-danger text-center">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {% if app.user %}
                <div class="mb-3">
                    You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Deconnexion</a>
                </div>
                {% endif %}

                <label for="inputEmail" class="fs-4 fst-italic"><strong>Email</strong></label>
                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                <label for="inputPassword" class="fs-4 fst-italic"><strong>Mot de passe</strong></label>
                <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

                <input type="hidden" name="_csrf_token"
                      value="{{ csrf_token('authenticate') }}">


                {# Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html #}

                <div class="checkbox mb-3">
                    <label>
                        <input type="checkbox" name="_remember_me"> Se souvenir de moi
                    </label>
                </div>

                <div class="mb-4">
                    <button class="mt-3 px-4 text-light fst-italic" style="font-size:1.5rem;background-color: rgb(9, 90, 9);
                        border:solid 2px #229296;border-radius:25px;" type="submit"><strong>Valider</strong>
                    </button>
                    <a href="{{ path('app_main_forgotten_password') }}" class="mx-4 my-4 px-5 py-1 btn btn-warning fst-italic"
                    style="color:lightgrey;font-size:1.5rem; border:solid 3px #229296; border-radius:50px;"><strong>Mot de passe oublié</strong></a>
                </div>

                <span class="fs-5 fst-italic">
                    <strong>Si vous n'avez pas encore de compte, vous pouvez en créer un en cliquant sur ce bouton: </strong>
                </span>
                <a href="{{  path('app_main_register') }}" class="m-3 px-3 py-1 text-light text-decoration-none fst-italic"
                    style="font-size:1.5rem;background-color: #11494C;border-radius:50px; border:solid 2px #229296;">
                    <strong>cliquez-ici</strong>
                </a>

            </div>

        </div>

        <a href="{{ path('app_blog_index') }}" class="px-3 py-2 mb-5 text-light fst-italic text-decoration-none fs-4"
            style="color:lightgrey;background-color:#229296;border-radius:50px;border:solid 3px #73e0ed;">
            <strong>Retour à la liste</strong>
        </a>

    </div>

{% endblock %}
