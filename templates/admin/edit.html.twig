{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

    <div class="container">

    <div class="titre">
        <span class="titre2">
            <strong>Modifier l'utilisateur</strong>
        </span>
    </div>

        <div class="cadre">

            <div class="cadre2">

                {{ include('admin/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                <a href="{{ path('app_admin_user_index') }}" class="inscription-valid btn btn-success">Retour à la liste</a>

                {{ include('admin/_delete_form.html.twig') }}

            </div>

        </div>

    </div>

{% endblock %}