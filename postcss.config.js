module.exports = {
    plugins: {
        // include whatever plugins you want
        // but make surte you install these via yarn on npm!

        // add browserlist config to package.json (see below)
        autoprefixer: {}
    }
}